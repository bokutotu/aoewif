module Aoewif.Compute (
    F32,
    Boolean,
    Index,
    Input,
    Output,
    Name (..),
    Dim,
    DTypeRep,
    f32,
    Tensor,
    Axis,
    Expr,
    Reducer,
    Compute,
    BlockM,
    ComputeIR (..),
    SymbolId (..),
    TensorId (..),
    BlockId (..),
    AxisId (..),
    ComputeNodeId (..),
    DimExpr (..),
    Symbol (..),
    DType (..),
    TensorKind (..),
    TensorDecl (..),
    AxisKind (..),
    AxisDecl (..),
    IndexExpr (..),
    ScalarLiteral (..),
    ComparePredicate (..),
    ScalarExpr (..),
    ReducerKind (..),
    Definition (..),
    ComputeBlock (..),
    tensorAt,
    blockAt,
    axisAt,
    program,
    dim,
    staticDim,
    input,
    output,
    block,
    spatial,
    reduction,
    define,
    reduce,
    sumOver,
    add,
    multiply,
    minimumReducer,
    maximumReducer,
    (!),
    boolean,
    index,
    indexLiteral,
    fma,
    min_,
    max_,
    exp_,
    log_,
    compare_,
    select,
    named,
) where

import           Aoewif.Internal.Compute
