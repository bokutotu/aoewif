module Aoewif.Compute (
    F32,
    Boolean,
    Index,
    Spatial,
    Reduction,
    R1,
    R2,
    R3,
    Name,
    Dim,
    Tensor,
    Axis,
    Expr,
    Computation,
    Entry,
    Program,
    ProgramM,
    Axes,
    IsShape,
    IsIndices,
    Comparison (..),
    ComputeError (..),
    program,
    dim,
    staticDim,
    input,
    compute,
    entry,
    (!),
    f32,
    boolean,
    index,
    indexLiteral,
    (.+.),
    (.-.),
    (.*.),
    (./.),
    fma,
    minimum,
    maximum,
    exp,
    log,
    compare,
    select,
    foldOver,
    named,
) where

import           Aoewif.Internal.Compute
import           Prelude                 ()
